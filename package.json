{
  "name": "squire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {"squire": "./mvp/run.mjs"},
  "engines": {"node": ">=24 <25"},
  "scripts": {
    "test": "node --check mvp/run.mjs && node --check mvp/sandbox-tool.mjs && node --test mvp/test/*.test.mjs"
  }
}
