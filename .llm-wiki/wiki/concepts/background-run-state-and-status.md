# Background Run State And Status

Reservation precedes remote lookup or sandbox work. Captured config/source identity binds detached handoff; serialized exact-owner claim permits one controller. State schema 2 has one attempt per Implement/Verify, immutable contract/candidate/evidence and terminal state. Separate status/watch historical readers display schema 1 without mutation or execution. Private outside-repository state/log paths, native owner observation and reservation ambiguity handling remain fail-closed.

See [Contract → Implement → Verify](contract-implement-verify-workflow.md).
