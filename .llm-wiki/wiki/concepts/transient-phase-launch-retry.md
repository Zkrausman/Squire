# Transient Phase Launch Retry

The only phase launch replacement is the existing typed pre-result allowlist: OS-confirmed EAGAIN without a child/output, or the exact bounded Codex Daybreak access failure protocol with no content/tools and zero usage. Maximum one generation; retain original deadline and exact clean HEAD before and after backoff. It cannot retry accepted reports, tests, malformed output or prior work. This exception does not create a model remediation loop.

See [Contract → Implement → Verify](contract-implement-verify-workflow.md).
